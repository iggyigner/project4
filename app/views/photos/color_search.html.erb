<ul class="photo-grid">
    <% @photos.each do |photo| %>
      <li>
        </div>
            <%#= photo.filename %>
            <%= link_to photo_path(photo) do %>
                <%= image_tag photo.image.url(:medium) %>
                <div>
                    <% if current_user %>
                        <%= link_to "Like", like_photo_path(photo), class: "add" %>
                    <% else %>
                        <%= link_to "Like", {:controller => 'auths', :action => 'new'}, data-reveal-id: "loginModal" %>
                    <% end %>
                </div>
            <% end %>
        </div>
      </li>
    <% end %>
</ul>

<div class="back-link">
    <%= link_to "Back", photos_path %>
</div>